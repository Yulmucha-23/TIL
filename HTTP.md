# HTTP 기본개념

### HTTP/HTTPS

- #### HTTP와 HTTPS의 차이
  - #### HTTP와 HTTPS의 가장 큰 차이점은 보안(암호화) 여부이다
    - HTTP: 데이터를 평문으로 전송하여 중간에 누군가 가로챌 경우 내용을 쉽게 볼 수 있다
    - HTTPS: 데이터를 암호화하여 전송하므로, 해커가 데이터를 가로채더라도 내용을 해독할 수 없다
  - #### 포트 번호:
    - HTTP는 기본적으로 80번 포트를 사용한다
    - HTTPS는 기본적으로 443번 포트를 사용한다
- #### HTTPS가 필요한 이유
  - HTTPS는 기존 HTTP에 암호화 기술인 SSL/TLS 프로토콜을 더한 통신 규약입니다  
    웹 서핑 중 주고받는 데이터가 유출되거나 조작되지 않도록 보호하기 위해서 필요합니다

### 요청 / 응답 구조

- #### Request (요청): 클라이언트가 서버에게 보내는 메시지
  - Start Line: 무엇을(URL), 어떻게(GET/POST 등 메서드) 할지 적습니다
  - Headers: 브라우저 정보, 쿠키, 인증 토큰 같은 부가 정보를 담습니다
  - Body: 서버로 보낼 데이터가 들어갑니다
- #### Response (응답): 서버가 클라이언트의 요청을 받고 보내는 답장
  - Status Line: 요청이 성공했는지 실패했는지 상태 코드를 보냅니다
  - Headers: 응답 데이터의 타입, 날짜, 서버 정보 등이 담깁니다
  - Body: 실제 데이터가 들어있습니다

### HTTP Method

- #### `GET`: 조회
  - 서버로부터 특정 데이터를 가져올 때 사용합니다
- #### `POST`: 생성
  - 서버에 데이터를 보내서 새로운 리소스를 만들 때 사용합니다
- #### `PUT`: 수정
  - 서버의 기존 데이터를 새로운 데이터로 갈아 끼울 때 사용합니다
- #### `DELETE`: 삭제
  - 서버의 특정 데이터를 삭제할 때 사용합니다

- #### GET과 POST의 차이
  - 데이터 위치: GET은 URL 주소창에 데이터를 노출해서 보내고, POST는 메시지 본문 안에 숨겨서 보낸다
  - 보안과 용량: GET은 보안에 취약하고 보낼 수 있는 양이 적지만, POST는 겉으로 드러나지 않고 대용량 데이터를 보내기에 적합하다
  - 기록: GET은 브라우저 히스토리나 즐겨찾기에 남지만, POST는 남지 않는다

- #### PUT과 POST의 차이
  - POST는 요청할 때마다 새로운 리소스가 추가된다
  - PUT은 동일한 요청을 여러 번 해도 결과가 항상 같다, 이미 있으면 통째로 덮어쓰고, 없으면 새로 만든다

- #### PUT과 DELETE의 차이
  - PUT은 기존의 데이터를 목적에 맞는 새 데이터로 갈아 끼우는 것이 목적이다
  - DELETE는 해당 데이터를 서버에서 완전히 없애는 것이 목적이다

### 상태 코드

- `200` (성공)
  - 요청이 성공적으로 처리되었을 때 쓴다 주로 데이터를 읽어오는 GET 요청에 대한 응답으로 보게 된다
- `201` (생성 성공)
  - 요청이 성공해서 새로운 데이터가 만들어졌을 때 쓴다 주로 게시글 작성 같은 POST 요청의 결과로 돌아온다
- `400` (잘못된 요청)
  - 요청 자체가 잘못되었을 때. 필수 항목을 빼먹었거나, 서버가 이해 못 할 형식으로 데이터를 보낸 경우
- `401`, `403` (인증 / 권한)
  - 로그인이 필요한데 안 되어 있거나 인증 정보가 틀렸을 때
  - 권한이 없을때
- `404` (찾을 수 없음)
  - 요청한 주소(URL)가 없거나 삭제되었을 때
- `500` (서버 오류)
  - 서버 내부에 버그가 있거나 예상치 못한 문제가 생겨서 응답을 못 해줄 때
